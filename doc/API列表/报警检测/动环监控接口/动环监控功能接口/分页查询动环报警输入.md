# 分页查询动环报警输入

> 接口说明
> 支持分页查询指定区域下的报警输入，可以选择是否包含下级区域

## 基本信息
|项目|值|
|---|---|
|接口版本|v1|
|适配版本|综合安防管理平台iSecure Center V1.3及以上版本|
|接口地址|`/api/pems/v1/alarm_in/search`|
|请求方法|`POST`|
|数据提交方式|application/json|

## 快速调用
```text
POST /api/pems/v1/alarm_in/search
```

## 请求参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`pageNo`|`body`|`integer`|是|页码， ( 0 , ~ )|
|`pageSize`|`body`|`integer`|是|每页大小， ( 0 , 1000 ]|
|`regionIndexCode`|`body`|`string`|否|区域indexCode，根据查询区域列表v2接口获取返回报文中的indexCode字段|
|`isIncludeSonRegion`|`body`|`boolean`|否|是否包含下级区域|
|`name`|`body`|`string`|否|报警输入名称|

## 请求参数示例
```json
{
  "pageNo": 1,
  "pageSize": 10,
  "regionIndexCode": "764e384b2df64f3eae92c3bbaa0127c2",
  "isIncludeSonRegion": true,
  "name": "Test"
}
```

## 返回参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`code`|`body`|`string`|否|返回码，0 – 成功，其他- 失败，参考附录E.4.3 动环错误码|
|`msg`|`body`|`string`|否|返回描述|
|`data`|`body`|`object`|否|返回数据|
|`+list`|`body`|`object[]`|否|查询结果集|
|`++indexCode`|`body`|`string`|否|报警输入编码|
|`++name`|`body`|`string`|否|名称|
|`++state`|`body`|`number`|否|报警输入状态,0：正常，|
|`+pageNo`|`body`|`number`|否|页码|
|`+pageSize`|`body`|`number`|否|页大小|
|`+total`|`body`|`number`|否|查询总数|
|`+totalPage`|`body`|`number`|否|总页数|

补充说明:
- 1：报警，2：正常，无数据传入

## 返回参数示例
```json
{
  "code": "0",
  "msg": "success",
  "data": {
    "list": [
      {
        "indexCode": "ae92c3bbaa0127c2764e384b2df64f3e",
        "name": "传感器",
        "state": 1
      }
    ],
    "pageNo": 1,
    "pageSize": 10,
    "total": 10,
    "totalPage": 1
  }
}
```
