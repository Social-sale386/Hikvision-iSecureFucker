# 分页查询动环环境量

> 接口说明
> 支持分页查询指定传感器下的环境量

## 基本信息
|项目|值|
|---|---|
|接口版本|v1|
|适配版本|综合安防管理平台iSecure Center V1.3及以上版本|
|接口地址|`/api/pems/v1/sensor/search`|
|请求方法|`POST`|
|数据提交方式|application/json|

## 快速调用
```text
POST /api/pems/v1/sensor/search
```

## 请求参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`pageNo`|`body`|`integer`|是|页码， ( 0 , ~ )|
|`pageSize`|`body`|`integer`|是|每页大小， ( 0 , 1000 ]|
|`type`|`body`|`string`|否|环境量类型，详见附录A.83 环境量类型|
|`name`|`body`|`string`|否|环境量名称，模糊查询|
|`parentIndexCode`|`body`|`string`|是|所属传感器编号，调用分页查询动环传感器接口获取返回参数indexCode|

## 请求参数示例
```json
{
  "pageNo": 1,
  "pageSize": 10,
  "type": "temp",
  "name": "RT",
  "parentIndexCode": "ask14cf1cf4jhvih0r12y8yhrch4y0"
}
```

## 返回参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`code`|`body`|`string`|否|返回码，0 – 成功，其他- 失败，参考附录E.4.3 动环错误码|
|`msg`|`body`|`string`|否|返回描述|
|`data`|`body`|`object`|否|返回数据|
|`+list`|`body`|`object[]`|否|查询结果集|
|`++alarmHigh`|`body`|`number`|否|偏高告警值|
|`++alarmLow`|`body`|`number`|否|偏低告警值|
|`++channelNo`|`body`|`number`|否|通道号|
|`++indexCode`|`body`|`string`|否|环境量编码|
|`++measureHigh`|`body`|`number`|否|量程上限|
|`++measureLow`|`body`|`number`|否|量程下限|
|`++name`|`body`|`string`|否|名称|
|`++parentIndexCode`|`body`|`string`|否|父设备编号|
|`++regionIndexCode`|`body`|`string`|否|所属区域编码|
|`++sensitive`|`body`|`number`|否|灵敏度|
|`++sensorType`|`body`|`string`|否|环境量类型,temp：温度 humidity：湿度 wind：风速 gas：气体浓度 voltage：电压 current：电流 frequency：频率 power：功率 flux：流量 batteryBackup：电池备份时间 lf：负载率 pf：功率因数 precipitation：降水量 dust：扬尘 noise：噪声 wl：水位 generalSensor：通用环境量 alarmPoint：告警点 windDirection：风向 pressure：压力|
|`++unit`|`body`|`string`|否|单位|
|`+pageNo`|`body`|`number`|否|页码|
|`+pageSize`|`body`|`number`|否|页大小|
|`+total`|`body`|`number`|否|查询总数|
|`+totalPage`|`body`|`number`|否|总页数|

## 返回参数示例
```json
{
  "code": "0",
  "msg": "success",
  "data": {
    "list": [
      {
        "alarmHigh": 10,
        "alarmLow": 1,
        "channelNo": 1,
        "indexCode": "3bbaa012ae92c7c2764e384b2df64f3e",
        "measureHigh": 10,
        "measureLow": 1,
        "name": "环境量",
        "parentIndexCode": "3bbaa012ae92c7c2764e384b2df64f3e",
        "regionIndexCode": "12ae923bbaa012c2764e384b2df64f3e",
        "sensitive": 0.01,
        "sensorType": "temp",
        "unit": "℃"
      }
    ],
    "pageNo": 1,
    "pageSize": 10,
    "total": 10,
    "totalPage": 1
  }
}
```
