# 窗口分割

> 接口说明
> 1.添加解码器和电视墙。
> 2.关联输出通道至电视墙上。
> 3.窗口已存在
> 4.调用该接口进行窗口分割

## 基本信息
|项目|值|
|---|---|
|接口版本|v1|
|适配版本|综合安防管理平台iSecure Center V1.4及以上版本|
|接口地址|`/api/tvms/v1/public/tvwall/floatWnd/division`|
|请求方法|`POST`|
|数据提交方式|application/json|

## 快速调用
```text
POST /api/tvms/v1/public/tvwall/floatWnd/division
```

## 请求参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`dlp_id`|`body`|`integer`|是|电视墙id，从获取电视墙大屏信息接口获取返回参数dlp_id|
|`floatwnd_id`|`body`|`integer`|是|窗口id，从获取电视墙大屏信息接口获取返回参数floatwmd_list下的id|
|`div_num`|`body`|`integer`|是|分割数，支持：|

补充说明:
- 1、2、4、6、8、9、12、16、25、36

## 请求参数示例
```json
{
  "dlp_id": 1,
  "floatwnd_id": 1,
  "div_num": 4
}
```

## 返回参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`code`|`body`|`string`|否|返回码，0 – 成功，其他- 失败，参考附录E.1.2 电视墙错误码|
|`msg`|`body`|`string`|否|返回信息|
|`data`|`body`|`object`|否|返回数据|
|`+id`|`body`|`number`|否|图层编号|
|`+left`|`body`|`number`|否|左坐标|
|`+top`|`body`|`number`|否|顶坐标|
|`+width`|`body`|`number`|否|图层宽度|
|`+height`|`body`|`number`|否|图层高度|
|`+full_larged`|`body`|`boolean`|否|是否全屏放大|
|`+enlarged`|`body`|`boolean`|否|是否处于放大状态|
|`+alarm_link_div`|`body`|`boolean`|否|是否进行报警联动分割|
|`+layer`|`body`|`number`|否|图层叠放序号，图层和图层之间可以重叠，值大的图层叠在值小的图层上。|
|`+dev_id`|`body`|`number`|否|设备中记录的图层编号|
|`+subwnd_num`|`body`|`number`|否|子窗口数|
|`+decoder_id`|`body`|`number`|否|所属解码器的编号|
|`+wndpos`|`body`|`number`|否|开窗所属查看器pos|
|`+wnd_id`|`body`|`number`|否|本地缓存的图层编号|
|`+uri`|`body`|`string`|否|绑定窗口uri,与电视墙相关，例：”api/tvwall/1/dlp/3/floatwnd/33624065”|
|`+attached_uri`|`body`|`string`|否|绑定设备uri,与设备相关，例：“api/tvwall/1/device/2/floatwnd/33624065”|
|`+dlp_id`|`body`|`number`|否|电视墙编号|
|`+openwnd_mode`|`body`|`number`|否|开窗模式: 0–统一坐标 1–分辨率开窗|
|`+dlp_row`|`body`|`number`|否|电视墙行数|
|`+dlp_col`|`body`|`number`|否|电视墙列数|
|`+device_wall_no`|`body`|`number`|否|设备墙号|

## 返回参数示例
```json
{
  "code": "0",
  "msg": "success",
  "data": {
    "id": 33624065,
    "left": 0,
    "top": 0,
    "width": 128,
    "height": 128,
    "full_larged": false,
    "enlarged": false,
    "alarm_link_div": false,
    "layer": 67108872,
    "dev_id": 16777217,
    "subwnd_num": 1,
    "decoder_id": 2,
    "wndpos": 1,
    "wnd_id": 16777217,
    "uri": "api/tvwall/1/dlp/1/floatwnd/33624065",
    "attached_uri": "api/tvwall/1/device/2/floatwnd/33624065",
    "dlp_id": 1,
    "openwnd_mode": 0,
    "dlp_row": 2,
    "dlp_col": 4,
    "device_wall_no": 1
  }
}
```
