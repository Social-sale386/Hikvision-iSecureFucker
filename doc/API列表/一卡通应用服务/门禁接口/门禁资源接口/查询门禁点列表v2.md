# 查询门禁点列表v2

> 接口说明
> 根据条件查询目录下有权限的门禁点列表。

## 基本信息
|项目|值|
|---|---|
|接口版本|v2|
|适配版本|综合安防管理平台iSecure Center V1.4及以上版本|
|接口地址|`/api/resource/v2/door/search`|
|请求方法|`POST`|
|数据提交方式|application/json|

## 快速调用
```text
POST /api/resource/v2/door/search
```

## 请求参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`name`|`body`|`string`|否|名称，模糊搜索，最大长度32，若包含中文，最大长度指不超过按照指定编码的字节长度，即getBytes(“utf-8”).length|
|`regionIndexCodes`|`body`|`string[]`|否|区域编号,可以为空;|
|`isSubRegion`|`body`|`boolean`|否|true时，搜索regionIndexCodes及其子孙区域的资源；|
|`pageNo`|`body`|`integer`|是|当前页码|
|`pageSize`|`body`|`integer`|是|分页大小|
|`authCodes`|`body`|`string[]`|否|权限码集合，详见附录A.3 资源权限码|
|`expressions`|`body`|`object[]`|否|查询表达式|
|`+key`|`Body`|`string`|否|资源属性名，key传updateTime，operator传between可以查询特定时间段更新的数据，考虑到校时和夏令时，建议值查询过去一天的数据变更|
|`+operator`|`Body`|`number`|否|操作运算符， 0 ：=， 1 ：>=， 2 ：<=， 3 ：in， 4 ：not in， 5 ：between， 6 ：like， 7 ：pre like， 8 ：suffix like|
|`+values`|`Body`|`string[]`|否|资源属性值，=、>=、<=、like、values数组长度只能是1； in、not in，values数组长度大于1，最大不超时20； in_array用于查询key值有多个value的情况，例如行车监控添加的设备类型为encodeDevice、encodeDeviceMss两个类型，使用encodeDevice或者encodeDeviceMss都可以查询到； between只能用于整形、日期 ； like只能用于字符串。|
|`orderBy`|`body`|`string`|否|排序字段,注意：排序字段必须是查询条件，否则返回参数错误|
|`orderType`|`body`|`string`|否|降序升序,降序：desc 升序：asc|

补充说明:
- String[]，支持根据区域批量查询；
- 区域编号个数
- <=1000个；
- 单个长度<=64Byte；，可从查询区域列表v2接口获取返回参数indexCode
- false时，只搜索 regionIndexCodes的资源

## 请求参数示例
```json
{
  "name": "test",
  "regionIndexCodes": [
    "8fyc8qw8280y0y43"
  ],
  "isSubRegion": true,
  "pageNo": 1,
  "pageSize": 1,
  "authCodes": [
    "view"
  ],
  "expressions": [
    {
      "key": "indexCode",
      "operator": 0,
      "values": [
        "ayd8y80y1y082ye01y2e8"
      ]
    }
  ],
  "orderBy": "name",
  "orderType": "desc"
}
```

## 返回参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`code`|`body`|`string`|否|返回码，0 – 成功，其他- 失败，参考附录E.other.1 资源目录错误码|
|`msg`|`body`|`string`|否|返回描述|
|`data`|`body`|`object`|否|返回数据|
|`+total`|`body`|`number`|否|记录总数|
|`+pageNo`|`body`|`number`|否|当前页码|
|`+pageSize`|`body`|`number`|否|分页大小|
|`+list`|`body`|`object[]`|否|资源数据列表|
|`++indexCode`|`body`|`string`|否|资源唯一编码|
|`++resourceType`|`body`|`string`|否|资源类型|
|`++name`|`body`|`string`|否|资源名称|
|`++doorNo`|`body`|`string`|否|门禁点编号|
|`++channelNo`|`body`|`string`|否|通道号|
|`++parentIndexCode`|`body`|`string`|否|父级资源编号|
|`++controlOneId`|`body`|`string`|否|一级控制器id|
|`++controlTwoId`|`body`|`string`|否|二级控制器id|
|`++readerInId`|`body`|`string`|否|读卡器1|
|`++readerOutId`|`body`|`string`|否|读卡器2|
|`++doorSerial`|`body`|`number`|否|门序号|
|`++treatyType`|`body`|`string`|否|接入协议|
|`++regionIndexCode`|`body`|`string`|否|所属区域|
|`++regionPath`|`body`|`string`|否|所属区域目录,以@符号分割，包含本节点|
|`++createTime`|`body`|`string`|否|创建时间|
|`++updateTime`|`body`|`string`|否|更新时间|
|`++description`|`body`|`string`|否|描述|
|`++channelType`|`body`|`string`|否|通道类型，door：门禁点|
|`++regionName`|`body`|`string`|否|区域名称|
|`++regionPathName`|`body`|`string`|否|所属区域目录名，符号"@"进行分隔|
|`++installLocation`|`body`|`string`|否|安装位置|

## 返回参数示例
```json
{
  "code": "0",
  "msg": "SUCCESS",
  "data": {
    "total": 3,
    "pageNo": 1,
    "pageSize": 1,
    "list": [
      {
        "indexCode": "df8w8cr800283c24c",
        "resourceType": "door",
        "name": "资源1",
        "doorNo": "123",
        "channelNo": "1",
        "parentIndexCode": "80d9099q9e991231",
        "controlOneId": "11111111",
        "controlTwoId": "2222222222",
        "readerInId": "ac789y2c0019c",
        "readerOutId": "arcew78c710",
        "doorSerial": 1,
        "treatyType": "hiksdk_net",
        "regionIndexCode": "d8a5476e-25c0-4aa2-b7e3-CC3788ba1f77",
        "regionPath": "@root000000@",
        "createTime": "2018-11-28T16:47:27:358+08:00",
        "updateTime": "2018-11-28T16:48:34:011+08:00",
        "description": "Test",
        "channelType": "door",
        "regionName": "acs_setUp_42054",
        "regionPathName": "@root000000@9ca1eef0-4579-4e7e-a601-caf486442d54@",
        "installLocation": "位置1"
      }
    ]
  }
}
```
