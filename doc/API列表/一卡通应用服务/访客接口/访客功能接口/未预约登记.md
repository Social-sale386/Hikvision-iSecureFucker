# 未预约登记

> 接口说明
> 该接口提供对访客进行直接登记和授权的能力，当访客没有预约就直接到访的情况下，允许接口调用方通过该接口实现对访客进行直接登记和权限授予。该接口的权限授予功能依赖于“查询访客权限组”接口，用户需要先在访客管理应用服务中配置好访客权限组。

## 基本信息
|项目|值|
|---|---|
|接口版本|v1|
|适配版本|综合安防管理平台iSecure Center V1.3及以上版本|
|接口地址|`/api/visitor/v1/orderless/register`|
|请求方法|`POST`|
|数据提交方式|application/json|

## 快速调用
```text
POST /api/visitor/v1/orderless/register
```

## 请求参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`receptionistId`|`body`|`string`|是|被访人唯一标识,查询人员列表v2接口获取返回报文中的personId字段|
|`visitEndTime`|`body`|`string`|是|预计离开时间，时间参数需满足ISO8601格式：yyyy-MM-ddTHH:mm:ss+当前时区，例如北京时间：2018-07-26T15:00:00+08:00预计离开时间必须晚于当前时间和预计来访时间|
|`personNum`|`body`|`string`|否|来访人数指同一批来访者的总人数，即主访客+随行人数。|
|`visitPurpose`|`body`|`string`|否|来访事由，长度为0～128个字符|
|`parkId`|`body`|`string`|否|园区id,不填使用默认园区|
|`visitorPermissionSet`|`body`|`object`|否|访客权限|
|`+privilegeGroupIds`|`Body`|`string[]`|否|访客权限组的id集合|
|`+defaultPrivilegeGroupFlag`|`Body`|`string`|否|是否使用默认权限组,1：使用；非1：不使用|
|`+designatedResources`|`Body`|`object[]`|否|资源点集合，数量不能超过1000|
|`++paramKey`|`Body`|`string`|否|参数类型(1:门禁点2:梯控楼层3:门口机4:围墙机5:停车场7:车道)|
|`++paramValues`|`Body`|`string[]`|否|参数集合(资源编码)|
|`visitorInfo`|`body`|`object`|是|访客信息|
|`+visitorName`|`Body`|`string`|是|访客姓名，支持中英文字符，不能包含 ‘ / \ : * ? “ < >|
|`+phoneNo`|`Body`|`string`|是|联系电话建议填写手机号码，仅支持1-20位纯数字|
|`+plateNo`|`Body`|`string`|否|车牌号，1-16位，不能包含 ‘ / \ : * ? “ < >|
|`+certificateType`|`Body`|`number`|否|证件类型，详见附录A.11 证件类型|
|`+certificateNo`|`Body`|`string`|否|证件号码，1~20个数字、字母组成；证件号码非空时，证件类型必填|
|`+certAddr`|`Body`|`string`|否|证件地址，支持1-128位字母、汉字|
|`+certIssuer`|`Body`|`string`|否|发证机关，支持1-32位字母、汉字|
|`+birthplace`|`Body`|`string`|否|籍贯，支持1-32位字母、汉字|
|`+visitorWorkUnit`|`Body`|`string`|否|来访单位，支持1-32位字符，不包含’/😗?”<>|
|`+cardNo`|`Body`|`string`|否|卡号，8-20位数字、大写字母|
|`+visitorPhoto`|`Body`|`string`|否|头像base64编码的字节流，图片最大200K，仅支持jpg格式。 访客头像需要秘钥加密传输，由于访客头像需要下发到设备，因此该接口会对访客头像进行质量检测，只有人脸评分大于等于75时，接口才会返回成功|
|`+visitorAddress`|`Body`|`string`|否|访客住址，支持1-128位中英文数字|
|`+plateColor`|`Body`|`number`|否|车牌颜色 0-蓝色 1-黄色 2-白色 3-黑色 4-绿色 5-民航黑色 255-其他|
|`registerPlace`|`body`|`string`|否|访客登记地点(长度不超过1024)|
|`receptionistOrgCode`|`body`|`string`|否|被访人所属组织code|

补充说明:
- 默认为1，不能小于1大于999

## 请求参数示例
```json
{
  "receptionistId": "3c094781-a9a0-4347-ba55-20794637db52",
  "visitEndTime": "2018-07-26T19:00:00+08:00",
  "personNum": "2",
  "visitPurpose": "参观",
  "parkId": "aaaaaa",
  "visitorPermissionSet": {
    "privilegeGroupIds": [
      "r164175b71751b75b73b61"
    ],
    "defaultPrivilegeGroupFlag": "1",
    "designatedResources": [
      {
        "paramKey": "1",
        "paramValues": [
          "AAAAA"
        ]
      }
    ]
  },
  "visitorInfo": {
    "visitorName": "张三",
    "phoneNo": "13574586231",
    "plateNo": "浙A12345",
    "certificateType": 111,
    "certificateNo": "312488198704015698",
    "certAddr": "杭州",
    "certIssuer": "市政府",
    "birthplace": "江苏",
    "visitorWorkUnit": "海康威视",
    "cardNo": "23355123233",
    "visitorPhoto": "/9j/4AAQKU1RVVldYWVp",
    "visitorAddress": "杭州滨江",
    "plateColor": 0
  },
  "registerPlace": "aaaa",
  "receptionistOrgCode": "aaaaa"
}
```

## 返回参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`code`|`body`|`string`|否|返回码，0 – 成功，其他- 失败，参考附录E.2.2 访客管理错误码|
|`msg`|`body`|`string`|否|返回描述|
|`data`|`body`|`object`|否|访客登记信息|
|`+QRCode`|`body`|`string`|否|动态二维码内容|
|`+orderId`|`body`|`string`|否|访客记录唯一标识，用于访客签离、已预约登记等操作|
|`+svrIndexCode`|`body`|`string`|否|图片存储服务的唯一标识|
|`+picUri`|`body`|`string`|否|访客头像，返回图片的相对Uri；需要调用“获取访客记录的图片”接口，来获取图片的数据|

## 返回参数示例
```json
{
  "code": "0",
  "msg": "success",
  "data": {
    "QRCode": " 1234567890",
    "orderId": "123dqwe-12eas12-asdq2wezxfqw-",
    "svrIndexCode": "1f276203e5234bdca08f7d99e1097bba",
    "picUri": "/pic?9dd351ia3-e*8634503d1165m3ep=t=i1p*i=d1s*i=d3b*i8da0*c74818542-9445fb--0874a79z189s=7ie4="
  }
}
```
