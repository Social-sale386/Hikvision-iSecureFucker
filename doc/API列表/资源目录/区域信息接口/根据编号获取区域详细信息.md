# 根据编号获取区域详细信息

> 接口说明
> 根据区域编号查询区域详细信息及总条数，主要用于区域详细信息展示。

## 基本信息
|项目|值|
|---|---|
|接口版本|v1|
|适配版本|综合安防管理平台iSecure Center V1.4及以上版本|
|接口地址|`/api/resource/v1/region/regionCatalog/regionInfo`|
|请求方法|`POST`|
|数据提交方式|application/json|

## 快速调用
```text
POST /api/resource/v1/region/regionCatalog/regionInfo
```

## 请求参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`indexCodes`|`body`|`string[]`|是|区域编号，最大1000个,根据查询区域列表v2接口获取返回报文中的indexCode字段|

## 请求参数示例
```json
{
  "indexCodes": [
    "91fa524346b544848d3b95df051f4CC6"
  ]
}
```

## 返回参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`code`|`body`|`string`|否|返回码，0 – 成功，其他- 失败，参考附录E.other.1 资源目录错误码|
|`msg`|`body`|`string`|否|接口执行情况说明信息|
|`data`|`body`|`object`|否|返回数据|
|`+total`|`body`|`number`|否|结果总数|
|`+list`|`body`|`object[]`|否|区域信息列表|
|`++indexCode`|`body`|`string`|否|区域唯一标识码|
|`++name`|`body`|`string`|否|区域名称|
|`++regionPath`|`body`|`string`|否|所属区域目录,以@符号分割，包含本节点|
|`++parentIndexCode`|`body`|`string`|否|父区域唯一标识码|
|`++available`|`body`|`boolean`|否|用于标识区域节点是否有权限操作|
|`++leaf`|`body`|`boolean`|否|标识区域节点是否叶子节点|
|`++cascadeCode`|`body`|`string`|否|级联平台标识|
|`++cascadeType`|`body`|`number`|否|区域标识|
|`++catalogType`|`body`|`number`|否|区域类型，|
|`++externalIndexCode`|`body`|`string`|否|外码(如：国际码)|
|`++parentExternalIndexCode`|`body`|`string`|否|父外码(如：国际码)|
|`++sort`|`body`|`number`|否|同级区域顺序|
|`++createTime`|`body`|`string`|否|创建时间，要求遵守ISO8601标准，如2018-07-26T21:30:08.322+08:00|
|`++updateTime`|`body`|`string`|否|更新时间，要求遵守ISO8601标准，如2018-07-26T21:30:08.322+08:00|

补充说明:
- 0：本级
- 1：级联
- 2：混合，下级推送给上级的本级点（杭州本级有滨江，然后下级滨江又把自己推送上来了，滨江是混合区域节点）
- 0：国际区域，
- 1：雪亮工程区域，
- 2：司法行政区域，
- 9：自定义区域，
- 10：历史兼容版本占用普通区域，
- 11：历史兼容版本占用级联区域，
- 12：楼栋单元

## 返回参数示例
```json
{
  "code": "0",
  "msg": "SUCCESS",
  "data": {
    "total": 1,
    "list": [
      {
        "indexCode": "root000000",
        "name": "根节点",
        "regionPath": "@root000000@",
        "parentIndexCode": "-1",
        "available": true,
        "leaf": false,
        "cascadeCode": "0",
        "cascadeType": 0,
        "catalogType": 10,
        "externalIndexCode": "34",
        "parentExternalIndexCode": "34",
        "sort": 1,
        "createTime": "2019-08-06T14:01:17.839+0800",
        "updateTime": "2019-08-07T14:01:17.839+0800"
      }
    ]
  }
}
```
