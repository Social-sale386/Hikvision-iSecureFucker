# 园区卡口违章事件查询v2

> 接口说明
> 简述：根据车牌号、事件、测速类型，违章类型查询违章事件，方便其他组件或第三方调用违章事件查询接口，方便用户查看。
> 编程引导：用户通过根据指定的开始时间和结束时间和分页的大小、车牌号（可选）、事件（可选）、测速类型（可选），违章类型（可选），调用“查询违章信息”接口，获取指定时间段内的违章信息列表。

## 基本信息
|项目|值|
|---|---|
|接口版本|v2|
|适配版本|综合安防管理平台iSecure Center V1.4及以上版本|
|接口地址|`/api/mpc/v2/illegal/events/search`|
|请求方法|`POST`|
|数据提交方式|application/json|

## 快速调用
```text
POST /api/mpc/v2/illegal/events/search
```

## 请求参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`pageSize`|`body`|`integer`|是|每页记录数,范围 ( 0 , 1000 ]|
|`pageNo`|`body`|`integer`|是|目标页码,范围 ( 0 , ~ )|
|`plateNo`|`body`|`string`|否|车牌号，支持模糊查询|
|`speedType`|`body`|`string`|否|测速类型，1（点位测速），2（区间测速）|
|`illegalType`|`body`|`string`|否|违章类型，1（超速），2（逆行），3（黑名单），5（违停）|
|`monitoringId`|`body`|`string`|否|卡口点id，不传代表查询全部， 支持多个indexcode , 逗号分割，可通过查询园区卡口点列表接口获取返回参数indexCode|
|`beginTime`|`body`|`string`|否|过车开始时间（格式：yyyy-MM-ddTHH:mm:ss.sss+当前时区）注：【过车开始、结束时间】和【入库开始时间、结束时间】只能配套使用，不能同时传值|
|`endTime`|`body`|`string`|否|过车结束时间（格式：yyyy-MM-ddTHH:mm:ss.sss+当前时区）注：【过车开始、结束时间】和【入库开始时间、结束时间】只能配套使用，不能同时传值|
|`createBeginTime`|`body`|`string`|否|入库开始时间（格式：yyyy-MM-ddTHH:mm:ss.sss+当前时区）注：【过车开始、结束时间】和【入库开始时间、结束时间】只能配套使用，不能同时传值|
|`createEndTime`|`body`|`string`|否|入库结束时间（格式：yyyy-MM-ddTHH:mm:ss.sss+当前时区）注：【过车开始、结束时间】和【入库开始时间、结束时间】只能配套使用，不能同时传值|
|`alarmReason`|`body`|`string`|否|布防原因，默认-1（全部原因），1（被盗车），2（被抢车），3（嫌疑车），4（交通违法车），5（紧急查控车）|
|`eventId`|`body`|`string`|否|事件的唯一编号|

## 请求参数示例
```json
{
  "pageSize": 20,
  "pageNo": 1,
  "plateNo": "浙A12345",
  "speedType": "-1",
  "illegalType": "-1",
  "monitoringId": "-1",
  "beginTime": "2019-07-26T15:00:00.000+08:00",
  "endTime": "2019-07-26T17:00:00.000+08:00",
  "alarmReason": "-1",
  "eventId": "561cd4e1b97a4b999d5fe4088381efd2"
}
```

## 返回参数
|参数|位置|类型|必填|说明|
|---|---|---|---|---|
|`code`|`body`|`string`|否|返回码，0 – 成功，其他- 失败，参考附录E.3.2 园区卡口错误码|
|`msg`|`body`|`string`|否|返回提示信息|
|`data`|`body`|`object`|否|返回数据|
|`+total`|`body`|`number`|否|总记录数|
|`+pageSize`|`body`|`number`|否|分页大小|
|`+pageNo`|`body`|`number`|否|页码|
|`+list`|`body`|`object[]`|否|记录数据集合|
|`++crossTime`|`body`|`string`|否|过车时间（格式：yyyy-MM-ddTHH:mm:ss.sss+当前时区）|
|`++eventId`|`body`|`string`|否|事件的唯一标识|
|`++plateNo`|`body`|`string`|否|车牌号|
|`++illegalType`|`body`|`number`|否|违章类型，1（超速），2（逆行），3（黑名单），5（违停）|
|`++speedType`|`body`|`number`|否|测速类型，1（点位测速），2（区间测速）|
|`++monitoringId`|`body`|`string`|否|事件源(卡口点的编号)|
|`++monitoringName`|`body`|`string`|否|事件源(卡口点的名称)|
|`++platePicUri`|`body`|`string`|否|车牌图片uri|
|`++carPicUri`|`body`|`string`|否|车辆图片uri|
|`++aswSyscode`|`body`|`string`|否|图片服务的唯一标识|
|`++reason`|`body`|`number`|否|布防原因(只有黑名单事件才会有值返回)，1（被盗车），2（被抢车），3（嫌疑车），4（交通违法车），5（紧急查控车）|
|`++uuid`|`body`|`string`|否|事件唯一标识|
|`++pointIntervalName`|`body`|`string`|否|区间/点位测速名称|
|`++speed`|`body`|`number`|否|车速(单位km/h)，大于0|

## 返回参数示例
```json
{
  "code": "0",
  "msg": "success",
  "data": {
    "total": 100,
    "pageSize": 10,
    "pageNo": 1,
    "list": [
      {
        "crossTime": "2019-07-28T15:00:00.000+08:00",
        "eventId": "cc2c881339ed4222839cb34a974655fd",
        "plateNo": "浙59SVN",
        "illegalType": 1,
        "speedType": 1,
        "monitoringId": "24522978916a4f0aa432508f01a12223",
        "monitoringName": "Channel-007",
        "platePicUri": "/pic?=d7ei703i10cd*73a-d5108a--22cd0c9d6592aiid=",
        "carPicUri": "/pic?=d7ei703i10cd*73a-d5108a--22cd0c9d6592aiid=",
        "aswSyscode": "h4h45y13ty23hg24h",
        "reason": 6,
        "uuid": "642C8DC7-259E-4276-9FD8-A70652468B1C",
        "pointIntervalName": "点位6000",
        "speed": 100
      }
    ]
  }
}
```
